# IM-Calculo — Regras do Projeto

Regras de negócio críticas que **SEMPRE** devem ser respeitadas ao alterar código, gerar queries ou calcular comissões neste repositório.

---

## Regras de comissão (carregadas automaticamente)

@.claude/rules/fator-comissao.md
@.claude/rules/comissao-corretor.md
@.claude/rules/comissao-integral-20.md

## Regras de sincronização (carregadas automaticamente)

@.claude/rules/sincronizacao-sienge.md
@.claude/rules/edge-cases-externos.md
@.claude/rules/numeracao-parcelas-sienge.md

## Regras de visualização e processo (carregadas automaticamente)

@.claude/rules/visualizacao-totais.md
@.claude/rules/leitura-de-listas-e-refetch.md
@.claude/rules/rodadas-b.md
@.claude/rules/ui-mobile-ios.md

## Regra de validação de contratos (pasta / 3ª fonte) — carregada automaticamente

@.claude/rules/validacao-contratos-pasta.md

---

## Princípios gerais

1. **Fórmula canônica do fator:** `Fcom = (Valor_Venda × Percentual_Total) / Valor_ProSoluto`. Nunca aplicar percentual direto na parcela.
2. **Comissão por pagamento, não por venda:** somar sempre das linhas de `pagamentos_prosoluto`, nunca de `vendas.comissao_corretor`.
3. **Comissão integral só quando entrada ≥ 20% paga à vista (não parcelada).**
4. **Nunca recalcular comissões de vendas antigas ao alterar percentuais.** O snapshot em `pagamentos_prosoluto.fator_comissao_aplicado` é a fonte da verdade histórica.
5. **Migrations 017 + 018 + 020** protegem linhas com `status = 'pago'`:
   - **Imutáveis em pago:** `tipo`, `valor`, `comissao_gerada` (financeiras/identidade).
   - **Editáveis em pago:** `data_pagamento` (020 — 2026-04-23, Sienge é fonte da verdade temporal); `fator_comissao_aplicado`, `percentual_comissao_total` (018 — 2026-04-21, snapshots/metadados).
   - **DELETE de pago:** bloqueado.
   - **Reversão pago→pendente:** só via fluxo explícito "Excluir Baixa" (`status='pendente'` + `data_pagamento=NULL` no mesmo UPDATE).

---

## Tabelas-chave

| Tabela | Propósito |
|--------|-----------|
| `vendas` | Dados da venda (valor, pro-soluto, fator canônico) |
| `pagamentos_prosoluto` | Parcelas geradas + snapshot de fator aplicado |
| `cargos_empreendimento` | Percentuais **atuais** por cargo por empreendimento |
| `cargos_empreendimento_historico` | Log de alterações de percentuais |
| `comissoes_venda` | Snapshot por venda (imutável) |

---

## Auditorias recentes

- [docs/auditorias/etapa0/p1-p2-execucao.md](docs/auditorias/etapa0/p1-p2-execucao.md) — P1 (fator de venda) e P2 (snapshot de pagamentos) executados em 2026-04-21
- [docs/auditorias/etapa0/p3-vendas-divergentes-decisao.md](docs/auditorias/etapa0/p3-vendas-divergentes-decisao.md) — 6 vendas divergentes aguardando decisão de negócio (causa: mudança de `tipo_corretor` com regeneração de grade)
- [migrations/018_afrouxar_snapshot_em_pago.sql](migrations/018_afrouxar_snapshot_em_pago.sql) — libera snapshots metadados em `status=pago` (trigger 017 afrouxado)
- [migrations/020_liberar_data_pagamento_sienge.sql](migrations/020_liberar_data_pagamento_sienge.sql) — libera `data_pagamento` em `status=pago` pra sync corrigir drift vs Sienge (2026-04-23)
- [migrations/021_corretor_cliente_id_origem.sql](migrations/021_corretor_cliente_id_origem.sql) — adiciona `corretor_id_origem` e `cliente_id_origem` em `vendas` pra proteger correções manuais contra sync (2026-04-27)
- [docs/auditorias/2026-05-13-drift/varredura-pagamentos-bagunca-2026-05-13.json](docs/auditorias/2026-05-13-drift/varredura-pagamentos-bagunca-2026-05-13.json) — varredura 2026-05-13: 99 vendas FIGUEIRA (30% do universo) com drift > 30d entre `data_prevista` e `data_pagamento` em pago e/ou `numero_parcela` duplicado. Causa-raiz: backfill antigo (`scripts/dry-run-backfill-income.mjs`) fez match heurístico apenas por `(venda_id, numero_parcela)` e nunca corrigiu `data_prevista` — só `data_pagamento`.
- [docs/rodadas/b7/b7-texto-para-usuaria.md](docs/rodadas/b7/b7-texto-para-usuaria.md) — rodada B.7 (2026-05-13): 11 vendas com `numero_parcela` duplicado (par cancelado+ativo). Aguardando re-baixa `/bulk-data/v1/income` (quota Sienge esgotou) pra reconciliação por `installmentId` real.
- [migrations/023_pagamentos_sienge_installment_id.sql](migrations/023_pagamentos_sienge_installment_id.sql) — adiciona `sienge_bill_id` e `sienge_installment_id` em `pagamentos_prosoluto` pra ancoragem 1:1 com Sienge (substitui match heurístico). **Não aplicada — pendente de revisão.**
- [docs/auditorias/2026-05-13-drift/aplicacao-data-prevista-2026-05-13.json](docs/auditorias/2026-05-13-drift/aplicacao-data-prevista-2026-05-13.json) — **132 parcelas em 16 vendas tiveram `data_prevista` corrigida pelo cache Sienge** (zero quota gasta, 2026-05-13). Drift entre 2 e 365 dias. Idempotente (rerun reporta 0 updated). Drift > 365d (19 parcelas) e sem-match (19) ficaram pra revisão humana.
- **Fix UI 2026-05-13** ([src/pages/AdminDashboard.jsx](src/pages/AdminDashboard.jsx), [CorretorDashboard](src/pages/CorretorDashboard.jsx), [ClienteDashboard](src/pages/ClienteDashboard.jsx), [HomeDashboard](src/pages/HomeDashboard.jsx), [comissaoCalculator](src/utils/comissaoCalculator.js)): parcelas `cancelado` agora renderizam corretamente (antes eram tratadas como `pendente`, inflando totais). `somarComissao` por default ignora canceladas.
- **Fix `propagarCronogramaCirurgico` 2026-05-13** ([AdminDashboard.jsx:181-184](src/pages/AdminDashboard.jsx#L181-L184)): a função antes filtrava `status !== 'pago'` (incluía canceladas como pendentes). Agora separa em 3 grupos (pagos/pendentes/cancelados) e canceladas são **ignoradas** — não entram em UPDATE nem DELETE. Elimina a causa-raiz que estava criando pares `cancelado+pendente` com mesma `numero_parcela` a cada regeneração de grade.
- [.github/workflows/recurring-reconciliation.yml](.github/workflows/recurring-reconciliation.yml) — **cron diário 08h BRT** que baixa income do Sienge (bulk-data, sem quota), gera plano de correção de `data_prevista` e aplica drifts pequenos (2-365d) automaticamente. Idempotente. Drifts >365d e sem-match ficam pra revisão humana. Falha o job se houver erros. **Pré-requisito:** configurar secrets no repo (VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, SIENGE_USERNAME, SIENGE_PASSWORD, SIENGE_SUBDOMAIN).
- [docs/rodadas/b6/aplicacao-b6-g1-2026-05-13.json](docs/rodadas/b6/aplicacao-b6-g1-2026-05-13.json) — **19 parcelas extras canceladas** (b6 Grupo 1, parcial): 14 da venda Fernanda c287 + 5 da Caroline c340.
- [docs/auditorias/2026-05-13-drift/plano-correcao-data-prevista-ampla-2026-05-13.json](docs/auditorias/2026-05-13-drift/plano-correcao-data-prevista-ampla-2026-05-13.json) + [docs/rodadas/b6/aplicacao-b6-g1-expandido-2026-05-13.json](docs/rodadas/b6/aplicacao-b6-g1-expandido-2026-05-13.json) — **varredura ampla 2026-05-13** sobre todas as 299 vendas FIGUEIRA com bill_id (não só as 99 da varredura inicial). Resultado: **96.6% das parcelas já corretas**. **+35 parcelas extras canceladas** em 3 contratos novos (c173 Carlos×3, c219 Josapha×1, c228 Letícia×31 — todas extras do gerador antigo, status pendente→cancelado, num > max_parcela_sienge). Algoritmo agora é genérico (não hard-coda contratos).
- [docs/auditorias/2026-05-14-reconciliacao-geral/pendencias-para-revisao-2026-05-14.md](docs/auditorias/2026-05-14-reconciliacao-geral/pendencias-para-revisao-2026-05-14.md) — **investigação 2026-05-14** dos casos da revisão humana:
  - **c236 → c390 (CLAUDIO MARTIRE)**: contrato 236 foi **reemitido** como 390 no Sienge. O sync já trouxe a venda 390 **completa e correta** (60 parcelas, 12 pagas, unidade 1008 C, comissão R$ 5.377,56). A venda 236 antiga é **duplicata obsoleta** com 3 parcelas pagas-fantasma — precisa ser eliminada mas o trigger bloqueia (tem pago). **Aguardando autorização da gestora pra eliminar a venda 236.**
  - **Mariane c165 + Andressa c8**: **renegociação** (não bug, não reemissão). Contratos seguem ativos no Sienge com o mesmo id; valores das parcelas batem (Mariane 60×1397.33=83.839,80 = Sienge PM; Andressa 60×1016.67+1000 = Sienge). Drift de "5 anos" era artefato de comparação posicional — o bill no income tem parcelas antigas+novas misturadas pós-renegociação (Mariane: 67 entradas pra 60 parcelas). Só o cronograma futuro (`data_prevista`) pode estar com datas do plano antigo — não afeta comissão. Corrigido em passagem: typo `data_pagamento='2202-02-18'` → `2026-02-18` na parc 8 da Mariane.

### Sessão 2026-06-01 — north star, estancar a sangria, medir o resíduo

- [docs/contexto/2026-06-01-north-star-reconciliacao.md](docs/contexto/2026-06-01-north-star-reconciliacao.md) — **norte único** da reconciliação: corretor/admin confia 100% no número porque o banco é espelho fiel do Sienge, atualizado sozinho. Modelo de 3 baldes (Truth In / Mirror Clean / Truth Out) + through-line de 6 passos. Termômetro-mestre: inadimplência exibida (~14% inflada → ~4,89% real).
- [docs/contexto/2026-06-01-plano-alinhamento-banco-sienge.md](docs/contexto/2026-06-01-plano-alinhamento-banco-sienge.md) — plano A–E (distrato, gerador idempotente, match ancorado, filas b9/b10). **Parcialmente executado.**
- **Parte B — gerador idempotente** ([AdminDashboard.jsx:~3408](src/pages/AdminDashboard.jsx)): `gerarPagamentosVenda` deixou de inserir cego. Opção B (skip-only não-destrutivo): só insere chaves `(tipo, numero_parcela)` inexistentes, nunca deleta/toca existente; grava `sienge_bill_id` quando a venda tem bill. Rodar 2× = no-op. Mata a causa-raiz de gêmeos novos.
- **Parte A.1 — ponte distrato no sync** ([sales-contracts.ts:411](supabase/functions/sienge-sync/normalize/sales-contracts.ts)): `situacao_contrato='3'` → `status='distrato'` + `data_distrato=cancellationDate`. Tolera reversão (reemissão volta a `pago`). É o **único caminho de sync vivo** (cron + botão Admin → edge function).
- **Parte A.3 (Admin)** ([comissaoCalculator.js](src/utils/comissaoCalculator.js), [AdminDashboard.jsx](src/pages/AdminDashboard.jsx)): novo helper `isVendaAtiva` (distrato/excluída não contam como ativa — **só p/ contagem, nunca p/ soma de comissão**, preserva R$684k pago). Auditoria de unidade ignora distrato; `matchStatus` mostra distrato em vermelho no "Todos". Decisões da gestora: comissão paga mantida · vermelho (não some) · todos os dashboards.
- **Sync legado DELETADO** — `src/services/sienge/` (17 arq: syncVendasV2, syncUtils, syncOrchestrator, etc.) + `SincronizarSienge.jsx` + css. Era **código morto** (nenhum import vivo; build verde provou). Eram implementação-fantasma duplicada do edge. **Agora há uma única via de sync.**
- [docs/contexto/2026-06-01-passo2-residuo-medido.md](docs/contexto/2026-06-01-passo2-residuo-medido.md) — **Passo 2: resíduo medido contra Sienge fresco** (dry-run de `reconciliar-todas-vendas.mjs`, bulk fresco 17.567 linhas). Resultado ancorado: **52 `parcela_entrada` pagas órfãs = R$ 21.194,06 de comissão contada em dobro** (não os ~R$27k do detector heurístico antigo, que inflava) + 5 pendentes-órfãs + 49 parqueadas (28 pro_soluto-negócio, 11 distrato-provável, ~10 ambíguo). Sienge verificado limpo (3.719 pagas/294 bills, par `(billId,installmentId)` único; `installmentId` reinicia por bill). Âncora local 80% limpa (0 dup pelo par correto).
- **Achado — cron passo ① legado:** `scripts/gerar-plano-correcao-data-prevista.mjs` lê arquivo **congelado** `docs/auditorias/2026-05-13-drift/varredura-pagamentos-bagunca-2026-05-13.json` (99 vendas) como escopo → as outras ~200 ficam invisíveis pra correção de `data_prevista`. Fix recomendado: fundir no passo ② (`reconciliar-todas-vendas`, universo completo) e aposentar o arquivo.
- **Pendente (Passo 3, gated — escreve em produção):** deploy do edge + backfill dos 25 distratos (A.2, nessa ordem); cancelar 5 pendentes-órfãs; "Excluir Baixa" das 52 pagas (R$21k); atacar as 49 parqueadas. A.3 Corretor/Cliente (cosmético).

### Sessão 2026-06-03 — buckets pra controladoria, dedup, auditoria do fator

- [docs/controladoria/conferencia-sienge-2026-06-02.xlsx](docs/controladoria/conferencia-sienge-2026-06-02.xlsx) — **planilha enviada à controladoria** (linguagem de negócio, coluna RESPOSTA): aba 1 parcelas a conferir (b9, 59), aba 2 saldo divergente (b10, 28), aba 3 distratos a confirmar (25), aba 4 análise interna (6 ambíguos). Cruzamento dos buckets: **7 vendas em b9∩distrato**; **c411/c334 do b10 = revenda de unidade distratada** (c181/c168).
- **Writes em produção (autorizados):** dedup 2 vendas manuais duplicadas "603"(CAYO→c411) + "1603"(HELOIZA→c422) — clientes reais, pagamentos conferidos no oficial; Excluir Baixa 4 pagas + cancelar 123 parcelas + soft-delete. Limpeza venda-teste "002" (jonas cliente) + **c236 CLAUDIO** (reemitido→c390, R$1.344 comissão fantasma) — Excluir Baixa 4 pagas + cancelar 116. Pós-fix: 0 vendas excluídas com paga, 0 parcela sem `data_prevista` em venda ativa.
- **2 vendas reais sem contrato Sienge** (Gabriel Adriano 412 + Gustavo 606, 0 pagas) → mandadas à controladoria confirmar (texto).
- [docs/contexto/2026-06-03-plano-reconciliacao-fator-comissao.md](docs/contexto/2026-06-03-plano-reconciliacao-fator-comissao.md) — **auditoria GERAL do fator** (275 vendas ativas, independente da THAI). Resultado: **`fator_comissao` da venda 100% correto** (externo 7,0% / interno 6,5%); **externos 100% íntegros**; **internos pendentes íntegros**; resíduo = **108 parcelas internas PAGAS / 11 vendas com `comissao_gerada` congelado = R$ 3.054,95 overpay** (imutabilidade travou a reescrita quando o fator foi corrigido). Composição de cargos documentada em [fator-comissao.md](.claude/rules/fator-comissao.md) (interno: Corretor 2,5 + Beton 1,25 + Nohros 1,25 + Ferretti 1,0 + Diretor 0,5).
- **Planilha THAI = pré-correção** (`docs/controladoria/porcentagem corretores(THAI).csv`): mede o **fator do CORRETOR**; "NOHROS"=100% internos, "CORRETA"=100% externos. "ATUAL 27%" era o estado antigo/total; o banco hoje já bate com "CORRETA" (1005 A = 11,67%). **Não confiar na coluna DEVIDA** (THAI errou 48/81).
- **Relatório da controladoria validado** (`admin@imincorporadora` → AdminDashboard `gerarRelatorioPDF` l.4557, abas "Relatórios"/"Ver PDF"): cálculo correto; decomposição por cargo proporcional. ⚠️ **Filtro de cargo importa:** "Total/Todos" mostra comissão TOTAL; pra ver o que o corretor recebe, filtrar cargo **"Corretor"**.
- **Frente #4 (outcome/repasse) ARQUIVADA** — repasse ao corretor (dias 10/20) é externo/incerto; não é fonte de verdade. Sistema é a fonte do pagamento (calcula sobre `status=pago` do income). Nenhum relatório foi enviado pelo sistema ainda → correção de fator pode ser retroativa (exceção auditada em [fator-comissao.md](.claude/rules/fator-comissao.md)).
- **Ação A APLICADA (2026-06-03):** [migrations/026_comissao_gerada_restaura_identidade.sql](migrations/026_comissao_gerada_restaura_identidade.sql) — `comissao_gerada` vira editável em pago **só pra restaurar a identidade** `=valor×fator_comissao_aplicado` (exceção cirúrgica, não afrouxa nada). + [scripts/B1-reconciliar-fator-interno-pago.mjs](scripts/B1-reconciliar-fator-interno-pago.mjs) `--apply`: **109 parcelas / 11 vendas internas corrigidas, R$ 3.054,99 overpay removido** (gravadas a 7% → 6,5% canônico). Causa-raiz: correção de percentual 7%→6,5% (~22/05) ficou pela metade — pagas travadas pela imutabilidade 017. Validado: 0 identidade quebrada (todas as pagas), idempotente (rerun=0). Saída: [docs/auditorias/2026-06-03-fator/B1-fator-interno-aplicado.json](docs/auditorias/2026-06-03-fator/B1-fator-interno-aplicado.json). **Fator agora 100% íntegro na carteira.**
- **Pendente (gated):** investigar 3 vendas com decomposição de cargo zerada (1206A/1408A/509A); aguardando respostas da controladoria (b9/b10/distrato/412/606).

### Sessão 2026-06-05 — curativo de maio, reorg de docs, e a fundação (âncora + gerador)

- **Curativo de maio (relatório mensal, produção autorizada):** o relatório que a Thaisy envia é **mensal** (pagos do mês). Descoberta grande: **quando um contrato é distratado, o Sienge dá baixa em TODAS as parcelas** (marca pagas) — só as pagas **antes** da data do distrato são reais. Os 3 distratos feitos **em maio** (c382/c279/c302) jogaram **R$77k de comissão falsa** no relatório de maio. Curado: cancelei 158 parcelas de baixa (mantendo o pago real pré-distrato, molde AMANDA c195), preenchi os pagamentos de maio que faltavam (8 sync-lag + 2 inserts de aditivo c150/c351 → [docs/contexto/2026-06-05-debito-aditivo-para-schema.json](docs/contexto/2026-06-05-debito-aditivo-para-schema.json)), removi a duplicata c129 p13. **Resultado: maio 208/209 reconciliado**, só a unidade 803 D (c275) em revisão. Métrica: [docs/historico/aplicacoes/2026-06-05-curativo-maio-execucao.json](docs/historico/aplicacoes/2026-06-05-curativo-maio-execucao.json).
- **Os 3 termos mapeados e conferidos contra relatórios oficiais do Sienge:** distrato **36** (relatório distrato), aditivo **15/14 contratos** (relatório reparcelamento + `/remade-installments`, batem 100%), cessão **3** (Extrato de cessão; 2 autocuram pelo sync). North Star #2: [docs/contexto/2026-06-05-north-star-2-tres-termos-DEFINITIVO.md](docs/contexto/2026-06-05-north-star-2-tres-termos-DEFINITIVO.md) + [mapa-3-termos.json](docs/contexto/2026-06-05-mapa-3-termos.json). **Juros de aditivo: NÃO comissionam** (decidido).
- **Deliverables controladoria:** planilha v2 enxuta (2 abas, saldo pré-classificado por aditivo) `conferencia-sienge-2026-06-05.xlsx`; CSVs por corretor (`PODE-MANDAR-HOJE` 35, `CASOS-A-RESOLVER` 24, `RESUMO-EXECUTIVO`); censo dos 76 corretores (49 com venda ativa Figueira; 21 sem venda — 3 são entidades de cargo NOHROS/BETON/FERRETTI, 2 testes).
- **Reorg de `docs/` (92MB → 4MB):** dumps RAW regeneráveis descartados (+ `.gitignore`), histórico em `docs/historico/`, vivos em `docs/contexto/`. Índice: [docs/README.md](docs/README.md).
- **O PIVÔ (a visão virou):** ao perguntar "faz sentido pensando no sync?", validamos que a fundação **NÃO** são as tabelas de evento (auditoria; o sync nem alimenta a cessão) — é a **ÂNCORA** (`sienge_installment_id` em toda parcela, hoje ~84%) + **materializar do income**. Plano vivo: [docs/contexto/2026-06-05-fundacao-ancora-gerador.md](docs/contexto/2026-06-05-fundacao-ancora-gerador.md). O [plano-estancar-sangria.md](docs/contexto/2026-06-05-plano-estancar-sangria.md) tem nota de pivô (Seções 4–5 superadas).
- **Fundação — Parte A (schema) FEITO:** migrations **027** (`pagamentos.renegociacao_id`), **028** (`vendas.cliente_id_origem += 'cessao'`), **029** (`pagamentos.motivo_cancelamento_parcela`). A 023 (índice UNIQUE da âncora) já estava aplicada.
- **Fundação — Parte B (reconciliador) endurecido:** [scripts/reconciliar-todas-vendas.mjs](scripts/reconciliar-todas-vendas.mjs) agora é **distrato-aware** (baixa `paymentDate >= data_distrato` não conta como pago) e **motivo-aware** (nunca reativa parcela cancelada de propósito). **Verificado rerun = no-op** → idempotente E **o cron diário não desfaz o curativo**. Marcadas 161 parcelas do curativo (160 `distrato` + 1 `duplicata`).
- **Fix do fator interno (produção, 2026-06-08):** ENZO interno (e +7 corretores) com relatório errado. **Causa-raiz:** a auditoria do 06-03 (B1) detectava por **identidade** (`comissao = valor × fator`), NÃO por **fator-correção** — **550 parcelas internas pagas a 7%** (externo) tinham identidade consistente (`comissao = valor × 7%`) → **invisíveis ao detector de identidade**. O "100% íntegro" do 06-03 era da **identidade**, não do **fator**. Corrigidas as 550 → 6,5% interno (fator+percentual+comissão no MESMO UPDATE, identidade preservada → migration 026 permite); **~R$15k overpay removido**. + 1 snapshot NULL (c405) + 399 arredondamentos de centavo (R$0,52). **Verificado: interno 0/0, externo 0/0** — fator E identidade 100% agora pela régua de **fator-correção** (`percentual == esperado(tipo)`), não só identidade. ⚠️ **Lição:** auditar fator por `percentual_comissao_total == (6,5 interno / 7 externo)`, não só pela identidade `comissao=valor×fator` (que é cega a 7% consistente em venda interna).
- **Pendente (fundação):** B2 (guards anchor-aware no gerador da UI `AdminDashboard.jsx`); Parte C (ancoragem 84%→100% = resolver as **49 vendas de revisão humana** — aditivos/dups/c275, gated por rodada-b). Os 3 termos (distrato backfill, aditivo wiring, cessão) são **derivados** que vêm depois da fundação ancorada.

### Sessão 2026-06-10/11 — North Star #3 (âncora correta) + a GRANDE cura: distrato R$887k + aditivo materializado

- [docs/contexto/2026-06-10-north-star-3-ancora-correta.md](docs/contexto/2026-06-10-north-star-3-ancora-correta.md) — **a 3ª pergunta do espelho:** Q1 Sienge-pago→banco-pago (reconciliador), Q2 banco-pago→Sienge-pago (over-pay), **Q3 a âncora é o PAR CERTO?** (caso MICHEL). "% preenchido" ≠ "% correto". F1 v2 segmentada por termo (decisão da gestão: LIMPO = valor+data EXATOS, sem tolerância): **97,83% do limpo correto**.
- **CURA DE DISTRATO EXECUTADA (produção, OK da gestão, 2026-06-11):** o curativo de maio tinha sido **desfeito pelo cron rodando código pré-merge** (endurecimento commitado 06-05 só chegou na main 06-09 à noite; crons 06-06→06-09 re-pagaram as 158 — forense: ainda carregavam `motivo='distrato'`). Escopo real medido: **35 distratos** (28 situacao=3 + **7 "limbo"** com situacao stale '2' no banco mas `Cancelado` confirmado via REST). Backfill de datas com ground-truth triplo (xlsx oficial ↔ **baixa em massa no income** ↔ REST `cancellationDate`; xlsx usa epoch padrão — o mapa-3-termos estava 1d adiantado). **1.908 baixas falsas canceladas = R$ 886.693,03 de comissão falsa removida; 100 pagas reais preservadas (R$ 46,5k)**. Verificado: rerun=0, pagas pós-distrato=0, âncora dup=0, reconciliador `reativar=0`. Scripts: [backfill-data-distrato.mjs](scripts/backfill-data-distrato.mjs) + [curar-distrato-apply.mjs](scripts/curar-distrato-apply.mjs); métricas em `docs/auditorias/2026-06-10-distrato/`.
- **F4 ADITIVO MATERIALIZADO (produção, OK da gestão, 2026-06-11):** mecânica validada no income — **reparcelamento dá baixa nas renegociadas NA DATA DO ADITIVO** (baixa de renegociação = primo do distrato; dinheiro rola pra grade gerada com installmentIds novos ~57+). 16 eventos/14 contratos via `/remade-installments` (REST, sem depender de export manual): **15 `renegociacoes` criadas, 43 baixas falsas (R$ 18.355,98) + 4 pendentes velhas → `cancelado` motivo='aditivo_renegociado'+`renegociacao_id`, 241 parcelas da grade nova INSERIDAS ancoradas (pendente, snapshot de fator, juros=0), 23 ancoradas, débito maio c150/c351 linkado**. Contabilidade exata: 241+23+2=266=installments gerados. Matcher income-driven (valor+dueDate EXATOS; numero_parcela NÃO serve — Sienge numera PM+balões juntos, banco separa). Script: [f4-materializar-aditivos.mjs](scripts/f4-materializar-aditivos.mjs).
- **Reconciliador agora ADITIVO-AWARE** ([reconciliar-todas-vendas.mjs](scripts/reconciliar-todas-vendas.mjs)): installments renegociados (via `renegociacoes.parcelas_originais`) **saem do universo** da venda — sem isso o sinal S2 (soma≠pro_soluto) parqueava toda venda com aditivo pra sempre e a grade nova nunca seria marcada paga. Com o filtro a soma fecha EXATA (juros=0). Resultado: S2 parqueadas 28→16 (as 12 destravadas; ficam c228+c351, sabidamente bagunçados), `reativar=0`, e o reconciliador consegue popular +553 âncoras no próximo run. **Relatórios não precisam de NADA**: já somam ignorando cancelado → grade velha some, nova entra pendente com datas reais.
- **c312 np3 curado cirurgicamente:** over-pay de âncora errada (backfill antigo gravou a baixa do balão B1 no row do B3/2028 não-pago) → Excluir Baixa + ancorado no inst 36 real. Lição no matcher do F4: fallback "pago na data do aditivo" **removido** (morde gêmeos mis-matched; só prevista==dueDate decide).
- **F3 FECHADO no mesmo dia (income de 05:11 via artifact):** o budget do bulk-data é ~1 pull/23h POR CONTA — fix estrutural: cron publica `.sienge-cache` como artifact (+ `sienge-cache-export.yml` sob demanda; `include-hidden-files: true` obrigatório). Dispatch do cron no branch aplicou a reconciliação aditivo-aware em produção: **+553 âncoras, 9 pagos da GRADE NOVA dos aditivos marcados (c150 62-65, c12 67-69, c351 67 — o dinheiro invisível do b10), reativar=0**. Re-medição com income do dia: **limpo 97,91%**, órfãs 0, **baixa de distrato no over-pay = 0** (era R$886k), sem-âncora 331→187. Resíduo final → rodada b11 (30 over-pay + 13 ambíguas) — **SUPERSEDIDA 2026-06-19** (arquivada em [docs/historico/rodadas/b11/](docs/historico/rodadas/b11/SUPERSEDIDA.md)): o snapshot envelheceu, a maioria já foi reconciliada após 11/06; over-pay real hoje é menor. A fila de revisão agora é **derivada do estado atual**, nunca de arquivo congelado ([scripts/analisar-conferencia-controladoria.mjs](scripts/analisar-conferencia-controladoria.mjs)). PR pra main: #31.
- **Pendências pequenas:** 17 data-diverge no limpo (R$11k de comissão, só datas — correção spec-permitida, aplicar num passo do cron); 1 paga real "antes do aditivo" (c150 inst 6, 2025-12-05) conferir; c228+c351 seguem parqueadas (revisão humana); decisão D1 (detector vira passo diário read-only do cron) em aberto.

### Sessão 2026-06-11 — cap-1000, refetch cirúrgico e visual distrato/aditivo (branch melhorias)

- **Auditoria de capacidade** (49 corretores + 6 internos simultâneos): infra aguenta com folga ~10× (Supabase PRO, queries ≤21ms, 13/60 conexões). Limite não se conta em usuários — conta-se em admins×mutações e linhas. Doc: `IM-demandas-futuras/docs/discovery/capacidade/2026-06-11-capacidade-e-north-star.md`.
- **Bug cap-1000 CORRIGIDO** ([supabaseQuery.js](src/utils/supabaseQuery.js) + nova regra [leitura-de-listas-e-refetch.md](.claude/rules/leitura-de-listas-e-refetch.md)): PostgREST corta em 1000 linhas silenciosamente; 4 corretores viam totais truncados (MATHEUS 2.930 parcelas → ~34%). `fetchMeusPagamentos` e `fetchTodosClientes` paginados. Explica o Ponto 1 da coleta spec-driven (conta Carlos Bruno = 2.559 parcelas).
- **Refetch cirúrgico no Admin**: `fetchData()` (26 queries/19k linhas) saiu dos fluxos quentes — baixa/excluir baixa fazem `update().select().single()` + merge; gerar pagamentos/renegociação re-leem só a venda (`refetchVendaEPagamentos`). Mount paginado com `.order('id')` (corrige ordem instável + break silencioso). **Sequência pro RLS: refetch escopado primeiro (feito), RLS depois — nunca o contrário.**
- **Visual distrato/aditivo (spec aprovado pelo owner)**: distrato é **visão segregada** — lista padrão NUNCA mistura; botão "Distratos" no Admin/Pagamentos e opção no filtro do Corretor/Minhas Vendas mostram SÓ elas (badge vermelho + data). Parcela com `renegociacao_id` ganha pill **"Aditivo"** roxa (admin + corretor). Comissão paga de distratada segue nos totais.
- **Pendente (gate de liberação pros corretores):** ligar RLS (agora destravado pela ordem certa) + re-validar Ponto 1 com a conta do Carlos pós-deploy.

### Sessão 2026-06-25 — drift de `data_prevista` zerado (o espelho passa a refletir o cronograma, não só o dinheiro)

- **Contexto (a correção honesta):** o "fechado" das rodadas anteriores olhava **soma de valor**, não **integridade do cronograma (datas)**. Investigação de 3 sintomas reais (1406 C "pula 6 meses", 903 C "repete vencimento de maio", 803 D "sempre com problema") provou via cache de income (casado por `bill+installment`): o salto do 1406 C e o adianto do 803 D eram **artefato NOSSO** (backfill antigo empurrou blocos inteiros de `data_prevista` meses à frente); o 903 C era **fiel** (a sobreposição de maio é reparcelamento real do Sienge). **Causa-raiz de processo:** o reconciliador **ancorava mas não realinhava `data_prevista`** das já-ancoradas — drift de backfill nunca era limpo. [docs/contexto/2026-06-19-north-star-data-drift-auto.md](docs/contexto/2026-06-19-north-star-data-drift-auto.md).
- **Varredura fleet-wide (read-only):** 14.500 parcelas ancoradas / 298 vendas → **98,0% exato** (`data_prevista==dueDate`). **293 com drift >2d (2,0%)**, concentrados em **10 vendas** (6 sistêmicas = bloco inteiro shiftado). Métrica: [docs/auditorias/drift-data-prevista-fleet.json](docs/auditorias/drift-data-prevista-fleet.json).
- **Correção aplicada em 3 camadas (produção, OK do usuário, 2026-06-25):**
  1. **254 AUTO** (250 pendentes + 4 pagas ≤30d) → `UPDATE data_prevista = dueDate` do Sienge, **só essa coluna**. Verificado: **254 updated / 0 erros**, **idempotente** (rerun=0 AUTO), invariantes limpas (pago_sem_data=0, pendente_com_data=0, sem_data_prevista=0). Plano completo de→para em [drift-data-prevista-plano-pre-apply.json](docs/auditorias/drift-data-prevista-plano-pre-apply.json) (snapshot pré-apply) + [drift-data-prevista-apply.json](docs/auditorias/drift-data-prevista-apply.json). Script: [scripts/aplicar-drift-data-prevista.mjs](scripts/aplicar-drift-data-prevista.mjs).
  2. **39 pagas >30d → rodada b12** (9 vendas) — **NÃO escrito**, vai pra gestora (régua: `data_prevista>30d` em paga exige revisão; pulo grande pode denunciar âncora errada — Q3/MICHEL). Entregável xlsx no formato **AÇÃO NECESSÁRIA** ([docs/controladoria/b12-realinhar-vencimento.xlsx](docs/controladoria/b12-realinhar-vencimento.xlsx)): 1 linha por parcela, com **Unidade · Cliente · Item · Vencimento (hoje) · Vencimento correto (Sienge) · Pago em**. Nomes vêm de `clientes.nome_completo` (`vendas.nome_cliente` é null). [docs/rodadas/b12/](docs/rodadas/b12/).
  3. **Furo tampado (camada 3):** [reconciliar-todas-vendas.mjs](scripts/reconciliar-todas-vendas.mjs) agora é **drift-de-data-aware** (anchor-driven): linha casada por `installmentId` com `data_prevista`/`data_pagamento` divergente → ≤30d **auto-cura** (PATCH só no campo de data, nunca valor/comissão/status), >30d → `revisao_data`. Loga `drift[]`. O cron diário (`--apply`, linha 112 do yml) passa a auto-corrigir drift de data ≤30d daqui pra frente — o one-shot só limpou o backlog.
- **Pendente:** resposta da gestora à b12 (39 pagas) → aplicar realinhamento; conferir as paridades "Pago em < Vencimento" em 1204 B/908 A (resquício duplo-fluxo/âncora, já roteado pra revisão).

### Sessão 2026-06-25 (cont.) — veredito 3-fontes + Excluir Baixa dos over-pay

- **Verificação dos 5 eixos (banco × Sienge × pasta), workflow adversarial (wf_884be8bd-624, 7 agentes):** veredito em [docs/auditorias/2026-06-25-veredito-reconciliacao-3-fontes.md](docs/auditorias/2026-06-25-veredito-reconciliacao-3-fontes.md). **Sem regressão** vs baseline 06-11 (números diferiram por **régua** — âncora-only/todas-as-limpas/R$ vs valor+data/ancorado-limpo/contagem — não por qualidade; recompute independente bateu casa decimal). Ancorado **subiu** 92,56%→94,21%. **Número honesto pro launch: 95,44% da comissão paga confirmada 1:1 contra o Sienge** (R$1,20mi de R$1,26mi); gap é majoritariamente **staleness de cache** (benigno) + tail por termo que a **pasta confirmou (62/70)**. Q1 reconciliador = resíduo zero. Inadimplência "flip" (Sienge>banco agora) = efeito de cura distrato/aditivo + cache 2d velho, não erro.
- **Achado adversarial (não silenciado):** divergência sistêmica **60 (pasta) × 61 (Sienge/banco)** em ~12 contratos — banco **espelha o Sienge** (não é bug), mas a entrada-total excede o contrato original por 1 parcela (~R$1.280). É pergunta Sienge-vs-contrato **pra controladoria**, não trava launch (afeta cronograma futuro, não comissão paga).
- **Excluir Baixa dos 6 over-pay confirmados (produção, OK do usuário):** [scripts/diag-overpay-baloes-sinais.mjs](scripts/diag-overpay-baloes-sinais.mjs) (evidência income×banco: banco 2 pagas, Sienge confirma 1) + [scripts/aplicar-excluir-baixa-overpay.mjs](scripts/aplicar-excluir-baixa-overpay.mjs) `--apply`. **4 balões** (1506A np5, 403B np2, 1105B np4, 1302A np2) → `pendente` (parcelas futuras reais que receberam baixa falsa do gêmeo pago); **2 sinais fantasma** (410A, 802A, sem âncora) → `cancelado` motivo `duplicata`. **R$ 7.167,79 de comissão duplicada removidos.** Verificado: pago_sem_data=0, pendente_com_data=0. Reversível ([rollback json](docs/auditorias/2026-06-25-excluir-baixa-overpay-rollback.json) + `--rollback`). Esses 6 eram os casos que derrubavam o eixo âncora-correta.
- **Status dos 5 eixos:** valor ✅ · status pago/pendente ✅ · data_prevista ✅ (hoje) · data_pagamento 🟡 (4 drift >30d → rodada-b) · **âncora-correta 🟡→subindo** (pós-Excluir-Baixa). Pendente menor: ancorar 1302A np1 (mantido) → inst9 do bill 258 (fecha o contrato); nota 60×61 à controladoria; re-medir com income fresco pra dissolver os 52 staleness.
- **b12 APLICADA (produção, OK do usuário, gestora indisponível → decisão pela verdade-3-fontes):** [scripts/aplicar-b12-realinhar-vencimento.mjs](scripts/aplicar-b12-realinhar-vencimento.mjs) `--apply` — **39 `data_prevista` de pagas realinhadas ao Sienge** (só a previsão; valor/comissão/data_pagamento/status intactos). Verificado nos 3 contratos-símbolo do usuário: **1406 C** (salto Fev→Ago: 3 pagas R$875,97 +153d → corrigidas) e **803 D** (5 pagas R$2.650 +31d → corrigidas) agora com **drift 0 e zero datas duplicadas** (o realinhamento sumiu com a colisão paga-vs-pendente da mesma data); **903 C** já estava fiel (drift 0 — o dobro-20/05 é reparcelamento real do Sienge, c228 known-messy). Idempotente (rerun=0), reversível ([rollback json](docs/auditorias/2026-06-25-b12-realinhar-rollback.json)).
- **Achado lateral (não-bug): 508 A tem 2 vendas = revenda.** c25 (bill 10, **distrato**, 0 pagas — cliente saiu) + c346 (bill 422, ativa, Michel). Padrão revenda-de-unidade-distratada (como c411/c334). Lição p/ scripts por-unidade: **iterar TODAS as vendas da unidade**, não `vs[0]` (a 1ª pode ser a do distrato) — fix aplicado no script da b12.

### Sessão 2026-07-29/30 — Portal do cliente completo + Boletos Sicoob EM PRODUÇÃO

- **Portal do cliente** ([ClienteDashboard.jsx](src/pages/ClienteDashboard.jsx)): visão financeira por compra derivada de `pagamentos_prosoluto` (pago/falta/% quitado/próxima parcela — corrige violação da regra de totais no ticker), extrato PDF s/ comissão ([extratoClientePDF.js](src/utils/extratoClientePDF.js)), edição de perfil + troca de senha, contato WhatsApp do corretor, e abas novas: **Pagamentos** (consolidado c/ filtros), **Meu Imóvel**, **Empreendimentos** (vitrine + modal de galeria por categoria + lightbox), **Garantia** (portais Solumn por empreendimento — migration 034, coluna `garantia_url`; Lotus sem link ainda).
- **Gestão de acesso do cliente pelo admin**: botão 🔑 no card (criar acesso/trocar senha) via edge function `admin-cliente-acesso` (service role; valida caller admin; **não troca mais a sessão** do navegador). Removido header `x-my-custom-header` do client supabase (quebrava CORS de edge functions).
- **Perf**: `fetchAllPaginated` ganhou `{ concurrency }` (lotes paralelos — 23k parcelas: 24→~4 round-trips; regra [leitura-de-listas-e-refetch.md](.claude/rules/leitura-de-listas-e-refetch.md) atualizada); fetchData do Admin num único `Promise.all`; overlay bloqueante só na 1ª carga.
- **BOLETOS SICOOB (Cobrança V3) — EM PRODUÇÃO**: tabela `boletos` (migrations 035/036 — ciclo de vida próprio, 1 boleto vivo/parcela, PDF no Storage; **NUNCA toca `pagamentos_prosoluto`**), edge function `sicoob-boletos` (emitir/consultar/2ª via/cancelar/webhook/armazenar-pdf), aba **Boletos** no Admin (por CLIENTE — mundo separado das comissões), strip de boleto no portal do cliente. Produção validada: mTLS e-CNPJ IM2 (validade **25/03/2027** ⚠️) + OAuth + contrato de cobrança **3771512** (coop 4368, conta 119.638-3). **1º lote real: 223 boletos / R$ 290.683,49** (planilha do financeiro cruzada 1:1 c/ parcelas pendentes; 16 divergências excluídas — parcelas pagas/canceladas/titular errado — devolvidas pra conferência) + 223 PDFs oficiais no Storage. **Emissão de produção roda como worker local** (edge runtime sem mTLS): runbook em [scripts/boletos/README.md](scripts/boletos/README.md).
- **Pendente (boletos)**: cadastrar **webhook** de liquidação no portal Sicoob (sem ele pagamento não marca `pago` sozinho); baixar boleto teste nossoNumero 11; devolutiva do financeiro dos 16 excluídos; decisão juros/multa; worker→GitHub Action p/ cron mensal.

### Sessão 2026-07-31 — a b12 foi DESFEITA pelo cron da `main` (regressão de `data_prevista`, 305 parcelas)

- **Gatilho:** usuário viu na UI a unidade **1406 C** com parcela 5 em **20/02/2026** e parcela 6 em **20/08/2026** (salto de 6 meses), e parcelas já **baixadas** com vencimento futuro até **out/2026**. O CLAUDE.md dizia que isso estava fechado desde 25/06 — estava, e voltou.
- **Prova em 3 fontes (contrato = Sienge ≠ banco):** o **contrato assinado** (`Torre C/Apto. 1406 .../termo---1406-C-pdf-D4Sign.pdf`, cláusula 4.2) diz *"50 parcelas de R$ 875,97 **iniciando em 20 de março de 2026**"* + 5×R$500 desde 20/10/2025 + 4 balões de R$10.000 (1º em 20/12/2026); soma = **R$ 86.298,50 = `valor_pro_soluto`** exato. O **Sienge** (bill 226) espelha isso instalment a instalment. **Só o banco** estava +5 meses a partir da np6.
- **Causa-raiz (provada por artifact, não por dedução):** o run do cron de **2026-07-11 03:53Z** rodou `scripts/aplicar-correcao-data-prevista.mjs --apply` — o **passo ① legado** — e gravou np6 `2026-03-20 → 2026-08-20`, np7 `→2026-09-20`, np8 `→2026-10-20`, alegando *"Sienge dueDate (billId=226 parc=6)"*. **Não era o dueDate do Sienge:** ele casa `numero_parcela` do banco com `installmentNumber` do Sienge (`"6/50"` = installmentId **11**), quando a âncora real é installmentId **6** (`"1/50"`). **Off-by-5 = as 5 parcelas de entrada.** Regra nova: [.claude/rules/numeracao-parcelas-sienge.md](.claude/rules/numeracao-parcelas-sienge.md).
- **Por que o passo legado ainda roda:** este worktree já o removeu do yml, mas **`schedule:` só dispara do branch default** — o cron executa a **`main`**, que ainda tem `Gerar plano de correcao` + `Aplicar plano` antes do reconciliador. **Branch não-mergeada = fix que não existe em produção** (mesma pegadinha que desfez o curativo de maio em 06-06→09). Esta branch está **4 commits à frente da main**.
- **Por que ninguém percebeu (o laço que se fecha sozinho):** deslocar datas em bloco faz a cauda repetir datas do miolo → dispara o guard **S4** (`parcelas ATIVAS com mesmo tipo/valor/data`) → o reconciliador ancorado **parqueia a venda inteira** → nunca corrige o drift, nunca marca baixa nova; e o passo legado vira **idempotente sobre o próprio erro** (07-31: `updated=0`). As **9 vendas em drift são exatamente as 8 travadas por S4** + 903 C. E há uma cegueira ainda mais básica: o `reconciliar-todas-vendas.mjs` **da `main`** é a versão **pré-drift** (312 linhas vs 353 daqui) — os campos `revisao_data`/`drift[]` **nem existem** no report que o cron produz. O endurecimento anchor-driven de 06-25 **nunca chegou a produção**.
- **Escopo medido (read-only, âncora `(bill,installmentId)` × cache income, universo completo):** **13.955/14.260 `data_prevista` exatas (97,86%)**; **305 em drift (2,14%) em 9 vendas** — 1204 B (48), 1607 A (48), 508 A (46), 908 A (46), **1406 C (42)**, 506 A (37), 803 D (18), 903 C (14), 1603 A (6); **43 delas em parcelas PAGAS**. Bate exatamente com as 305 que o passo legado gravou em 11/07.
- **Dano colateral confirmado:** com a venda parqueada, baixa real do Sienge não entra. **1406 C inst 9** (venc 20/06/2026, **pago 19/06/2026**, R$ 875,97 / R$ 306,59 de comissão) segue `pendente` no banco. Em 506 A e 903 C há mais candidatas, mas essas duas têm aditivo/reparcelamento — exigem o filtro de `renegociacoes` antes de afirmar.
- **⚠️ A branch está 4 commits À FRENTE e 83 ATRÁS da `main`.** Merge direto desta branch **não é o caminho** — os arquivos de `src/` daqui são anteriores a 83 commits de produto (ClienteDashboard, CadastroFigueira, sicoob-boletos, utils de PDF, testes) e uma resolução de conflito errada reverteria trabalho alheio. O caminho seguro é **PR cirúrgico a partir da `main`**: branch nova de `origin/main` mexendo **só** em (a) `.github/workflows/recurring-reconciliation.yml` — remover os passos `Gerar plano de correcao` + `Aplicar plano`; e (b) opcionalmente portar `scripts/reconciliar-todas-vendas.mjs` na versão endurecida. Zero toque em `src/`.
- **NADA foi escrito em produção nesta sessão.** Ordem correta — **desarmar antes de curar**: (1) PR cirúrgico na `main` tirando o passo legado do cron; (2) só então reaplicar as 305 datas pela âncora; (3) conferir o artifact do cron seguinte provando que não reverteu; (4) destravar o S4 e deixar o reconciliador marcar as baixas perdidas. Curar antes de (1) = o cron desfaz na madrugada seguinte.
- **Terceira prova (o mesmo conserto morreu 2x):** `docs/controladoria/wf-data-rollback.json` (mtime **10/07 14:00**) guarda 306 linhas de `de` — alguém rodou `aplicar-drift-ancora.mjs --apply`; às **14:02** a revarredura deu **plano vazio** (drift zerado, verificado). **13h49 depois**, o cron reescreveu 305. O `de` guardado pra 1406 C np6 é `2026-08-20` — exatamente o valor de hoje. Antes disso a b12 (25/06) já tinha morrido do mesmo jeito.
- **DRY-RUN 2026-07-31** ([scripts/realinhar-data-prevista-ancora.mjs](scripts/realinhar-data-prevista-ancora.mjs), read-only, escopo derivado do estado): **266 AUTO** (262 pendentes + 4 pagas ≤30d) · **39 rodada-b** (pagas >30d, gate da spec) — das quais **37 são byte-idênticas ao de→para já aprovado na b12**; só 2 são novas (**903 C np4** 2025-11-20→2026-05-20, c228 known-messy; **1607 A np12** 2026-12-20→2026-07-20). Plano em `docs/auditorias/realinhar-data-prevista-2026-07-31-plano.json`. ⚠️ O script existente `aplicar-drift-ancora.mjs` **não tem o gate >30d** — aplica pago sem revisão, contra a spec; preferir o novo.
- **O conserto é tudo-ou-nada:** medido caso a caso, aplicar **só as AUTO** deixa **8 das 9 vendas ainda travadas** no S4 (e piora 803 D, 1→4 colisões). **AUTO + rodada-b → 0 colisões nas 9**, o que **destrava o reconciliador** pra ele mesmo marcar as baixas perdidas (ex.: 1406 C inst 9). Correção parcial não resolve.
- **APLICADO 2026-07-31 (produção, OK do usuário — `--sem-pagos`):** **253 `data_prevista` de PENDENTES realinhadas** ao Sienge pela âncora. Decisão do usuário: **nenhuma linha `pago` tocada**, nem as 4 que a spec permitiria (drift ≤30d) — pagamento real só se mexe caso a caso. Só a coluna `data_prevista`; `valor`/`comissao_gerada`/`status`/`data_pagamento` intactos. Verificado: `pago_sem_data=0`, `pendente_com_data=0`, rerun idempotente, **0 linhas pagas tocadas** (as 2 pagas alteradas na janela são 905 B e 606 A — lançamentos de julho feitos no app, fora do escopo). Espelho **97,86% → 99,64%**. Rollback: `docs/auditorias/realinhar-data-prevista-2026-07-31-rollback.json`.
- **803 D — o off-by-one era em `data_pagamento`, NÃO na âncora (diagnóstico corrigido no mesmo dia):** o lote inicial incluía 9 pendentes da 803 D e **foram revertidas por precaução**, porque as pagas do banco casavam com o `paymentDate` do installment **seguinte** (np2 pago 2025-11-10 = inst 3). A leitura na hora foi "âncora deslocada". **Estava errada:** a âncora sempre esteve certa — quem estava deslocado um mês era o `data_pagamento`. O **sync via edge function** (passo 2 do cron, 14:55) reescreveu os `data_pagamento` da 803 D alinhando-os ao installment correto, e a auditoria de âncora passou a dar **10 batem / 0 off**. Com isso a 803 D deixou de ser exceção e entrou no lote final. Lição: `data_pagamento` divergente **não prova** âncora errada — testar as duas hipóteses antes de rotular.
- **⚠️ Terceiro escritor descoberto — o sync escreve em `pagamentos_prosoluto`:** não são só os scripts. O passo `Sincronizar vendas Sienge` (edge `sienge-sync` → `normalize/sales-contracts`) **atualiza parcelas**, inclusive `data_pagamento` de linhas **pagas** (run de 31/07: `pagamentos: {inseridos: 0, atualizados: 4, pagos_preservados: 2489}`). Ele agiu **corretamente** aqui (curou a 803 D), mas qualquer forense de "quem mexeu nessa linha" precisa considerar **três** escritores: reconciliador, sync/edge, e (até hoje) o passo legado.
- **Fila de revisão humana — 43 pagas** (`docs/rodadas/b13-pagas-realinhar-vencimento.json`): 1204 B 7 · 1607 A 7 · 908 A 6 · 1603 A 6 · **803 D 9** · 508 A 3 · 1406 C 3 · 903 C 1 · 506 A 1. Destas, **37 têm de→para byte-idêntico ao já aprovado na b12**; 6 são novas. **Nas 34 fora da 803 D, a `data_pagamento` corrobora a data do Sienge** (ex.: 1406 C np6 vence "20/08" no banco mas foi paga em **20/03** — a data do Sienge). Na 803 D é o contrário (o pagamento bate com a data do banco) — pelo off-by-one acima.
- **CRON DESARMADO — PR [#60](https://github.com/Neskrux/IM-Calculo/pull/60) MERGEADO na `main` (2026-07-31, OK do usuário):** sequência executada — (1) `gh workflow disable` protegeu as 253 na hora; (2) PR cirúrgico saindo de `origin/main` tocando **1 arquivo** (`recurring-reconciliation.yml`, 223→200 linhas), removendo `Gerar plano de correcao` + `Aplicar plano`; (3) checks verdes, squash-merge; (4) `gh workflow enable`; (5) `workflow_dispatch` de validação **imediato** em vez de esperar a madrugada. **Resultado do run 30640591792: `success`, sem os passos legados, artifact SEM `aplicacao-data-prevista-*.json`, `okMarc=7` (7 baixas reais registradas), 0 correção de data — e o espelho continuou em 99,64%.** Primeira vez que uma correção de `data_prevista` sobrevive a um ciclo do cron. Nota: main's reconciliador já tinha `SIENGE_*` no próprio `env` (verificado) — nenhuma alteração extra foi necessária.
- **Ainda travadas por S4 (8 vendas):** 1603 A, 1406 C, 1607 A, 803 D, 508 A, 506 A, 1204 B, 908 A. Só destravam quando as **43 pagas** forem decididas (b13) — as pendentes sozinhas não resolvem a colisão. Efeito visível na UI enquanto isso: em 1406 C a parcela 9 (pendente) vence **20/06/2026** e a parcela 6 (paga) segue marcada **20/08/2026** — a lista aparece fora de ordem. É esperado e some com a b13.
- **FECHADO — espelho de `data_prevista` em 100,00% (14.260/14.260):** com o cron já desarmado, o usuário autorizou mexer nas pagas ("se elas têm data diferente do contrato"). Aplicadas as **52 restantes** (`--incluir-pagos`): 8 pendentes + **44 pagas**. **Gate de corroboração antes de gravar: nas 44 pagas, a data nova ficou MAIS PERTO do pagamento real em 44/44 — nenhuma piorou.** Verificado: `pago_sem_data=0`, `pendente_com_data=0`, rerun idempotente (drift 0), **as 9 vendas saíram do S4** (as 5 colisões que restam na frota são de outras vendas — 1003 C, 1302 A, 603 D, 802 A, 810 C, balões duplicados pré-existentes → nova fila). Total do dia: **305 `data_prevista` realinhadas** (253 + 52). Rollback em `docs/auditorias/realinhar-data-prevista-2026-07-31-rollback.json`.
- **1406 C — o caso que abriu a sessão, fechado:** parc 6 venc **20/03/2026** paga 20/03 · parc 7 venc 20/04 paga 20/04 · parc 8 venc 20/05 paga 18/05 · parc 9 venc 20/06 pendente. Bate com o contrato assinado (cl. 4.2), com o Sienge e com o dinheiro. Sem salto, sem parcela "paga com vencimento futuro".
- **Resposta ao "o Sienge está mandando data errada?": NÃO.** Nas 3 fontes o Sienge sempre esteve certo — o contrato assinado confirma (`50 parcelas de R$ 875,97 iniciando em 20/03/2026`) e a `data_pagamento` do cliente confirma. **O erro era 100% nosso**, gravado pelo passo legado. Corrigir foi trazer o banco de volta ao que o contrato diz. Nenhuma requisição ao Sienge foi gasta: o realinhamento lê só do `.sienge-cache`.
- **Guarda-corpo novo no script:** [realinhar-data-prevista-ancora.mjs](scripts/realinhar-data-prevista-ancora.mjs) agora **audita a âncora sozinho** antes de gravar (compara `data_pagamento` do banco com o `paymentDate` do installment ancorado vs. o seguinte) e **exclui do apply** qualquer venda em que a maioria das pagas case com `inst+1`. Flags: `--sem-pagos` (só pendentes) · `--incluir-pagos` (inclui pagas >30d, exige corroboração) · `--rollback`.
- **Lição de processo (a causa-raiz da causa-raiz):** o problema não é o script legado — é a **branch de vida longa**. O conserto existia, verificado, desde 25/06, e nunca alcançou produção. Ver [[cron-roda-da-main]] na memória.

### Sessão 2026-08-04 — Perfil do imóvel no portal do cliente: contrato, unidade, plantas e documentos

- **Meu Imóvel enriquecido** ([ClienteDashboard.jsx](src/pages/ClienteDashboard.jsx)): bloco "Dados do contrato" (nº contrato, data, valor, entrega, corretor c/ WhatsApp), chips da unidade lendo da tabela `unidades` via `compra.unidadeInfo` (os antigos `compra.bloco/andar` eram colunas inexistentes — código morto), seção **Plantas** destacada (categoria `planta`, fundo branco + contain). 298/304 vendas com contrato+unidade+entrega; 561 unidades com área; 0 fotos `planta` cadastradas ainda (upload manual — Sienge não fornece imagens).
- **Documentos do imóvel e da venda** (migration [037](migrations/037_documentos_empreendimento_venda.sql), aplicada): `empreendimento_documentos` (registro, matrícula, alvará, memorial...) e `venda_documentos` (contrato assinado, aditivo...), ambas com `visivel_cliente`. Bucket `documentos`. Admin: galeria virou "Mídia e Documentos" (abas Fotos|Documentos, [EmpreendimentoDocumentos.jsx](src/components/EmpreendimentoDocumentos.jsx)); Visualizar Venda ganhou aba Documentos ([VendaDocumentos.jsx](src/components/VendaDocumentos.jsx)). Portal: pills de download no bloco de contrato + seção Documentação no modal do empreendimento.
- **Perfil de teste** (brunosandoval@investmoneysa.com.br, cliente 205faad2, venda bdba3f68 = TESTE-0001, unidade teste "1501 A" LOTUS s/ sienge_unit_id): grade financeira realista pendente (SQL no scratchpad, bloqueado pelo classificador).

### Sessão 2026-08-05→12 — Segundo banco de boletos: AILOS (085) homologado, EM PRODUÇÃO

- **Decisão:** emissão seleciona o banco (Sicoob OU Ailos). Tabela `boletos` já era multi-banco (`(banco, nosso_numero)` único); Ailos entra com `banco='ailos'`.
- **Arquitetura Ailos ≠ Sicoob:** SEM mTLS. WSO2: token do CLIENT (client_credentials, ~1h) + code do COOPERADO (login interativo 1x → callback edge `ailos-boletos/callback` c/ anti-CSRF `state_pendente`; refresh sem interação). Estado em `ailos_tokens` (migration [038](migrations/038_ailos_tokens.sql), aplicada). Hosts: hml `apiendpointhml.ailos.coop.br` · prod `apiendpoint.ailos.coop.br`. Ambiente no worker: `AILOS_ENV=producao` → `.env.ailos.producao` (gitignored).
- **⚠️ API NÃO devolve PDF** — devolve codigoBarras/linhaDigitavel/pix.qrCode(base64); layout é nosso: [ailos-boleto-pdf.cjs](scripts/boletos/ailos-boleto-pdf.cjs) (Febraban, ITF-25 desenhado, logo oficial embutida c/ alias+compress ~77KB, QR Bolepix). Regenerar sem re-emitir: [ailos-regerar-pdfs.cjs](scripts/boletos/ailos-regerar-pdfs.cjs).
- **HOMOLOGAÇÃO APROVADA (08-10)** (3 PDFs c/ logo) + **TESTE DE PRODUÇÃO LIQUIDADO (08-11/12)**: 2 boletos reais R$10 — nº1 Bolepix pago via Pix (situacao=5 ✅) e nº2 pago por código de barras (compensou Nuclea D+1, situacao=5 ✅). Lições do validador: `descricaoDocumento` ≤15 chars; espécie 4=MENS; formaEmissao 2; juros/multa tipo 3 (isento, igual Sicoob).
- **Worker de lote** [ailos-emitir-lote-excel.cjs](scripts/boletos/ailos-emitir-lote-excel.cjs): mesmas planilhas e match-exato do Sicoob; trava 1-boleto-vivo/parcela vale ENTRE bancos (dry-run validado c/ planilhas de julho: 223 pulados); PDF armazenado via `sicoob-boletos/armazenar-pdf` (rota agnóstica).
- **UI multi-banco (aba Boletos):** card "Emissão em massa" — seletor Sicoob 756 | Ailos 085, download dos modelos de planilha por banco (xlsx client-side, chunk lazy) e **importação da planilha preenchida** c/ conferência match-exato na tela (mostra lote pronto + fora com motivo; NÃO emite — baixa lote conferido pro robô).
- **Pendente:** webhooks v2 Ailos deram **403** (API não liberada na subscription — pedir habilitação; eventos 2=Liquidado/1=Baixado → rota `/webhook` já deployada); emissão pontual Ailos via edge (precisa secrets AILOS_* no Supabase); webhook Sicoob; decisão juros/multa.
- **1º LOTE REAL AILOS EMITIDO (2026-08-14):** 70 boletos / R$ 93.911,36, zero erros, 70 PDFs homologados no Storage, 70 QR Pix — dry-run bateu 1:1 com a conferência da UI. Conferência da UI agora explica CADA exclusão com dados do sistema (já paga em DD/MM · planilha vs sistema · titular sem CPF · boleto vivo em outro banco). Resíduo do lote (13): 8 titulares sem CPF no cadastro, 3 divergências valor/data (508 A/906 C/803 D), 2 parcelas já pagas na planilha (1303 C p1, 1407 D p3). Relatório: artifact 76f27bda. **Próximo destravamento:** usuário rodar `vault.create_secret` (AILOS_CONSUMER_KEY/SECRET) no SQL Editor → construir botão "Emitir" no modal (edge, sem robô).
- **EMISSÃO AILOS PELA TELA — EM PRODUÇÃO (2026-08-17):** botão "Emitir N boletos" no modal de conferência. Edge `ailos-boletos` rota `/emitir-lote` (admin JWT; reconfere pendente+sem-boleto-vivo+endereço; emite V2 bolePix; grava `boletos`) + `/armazenar-pdf`. Segredos no Vault (RPC `ailos_segredo`, migration 039 — SECURITY DEFINER só service_role). **Lição cara: jsPDF na edge estoura CPU (HTTP 546 "CPU Time exceeded") mesmo em blocos de 8** → PDF é gerado NO NAVEGADOR ([src/utils/ailosBoletoPdf.js](src/utils/ailosBoletoPdf.js), porta ESM do layout homologado; logo em `src/imgs/ailos-logo.png`) e enviado; front chama 1 boleto por vez, barra de progresso "N de M · ref", falhas listadas por boleto c/ orientação, toast "X de Y emitidos", grupo "🔁 Duplicados" explícito. Idempotente entre tela↔robô (lote fev/27: 36 pela tela + 34 pelo robô = 70/70, R$ 93.911,36, 70 PDFs). Sicoob segue só pelo robô (mTLS).
- **2 lotes reais Ailos no ar:** set/26 (70, 14/08) + fev/27 (70, 17/08). Resíduo recorrente do financeiro: 8 titulares sem CPF no cadastro (Adryelle 1806D, Diego 401A, Felix 1307D, Gonçalves 1603D, Jader 609A, Miguel 803B, Valdemir 1702C, +1), 3 divergências data (906C/508A/803D), 1403B com CPF diferente (Cleuci).

### Sessão 2026-09-04 — papel Coordenador na conta do corretor (Pires, Carolina, Jessica)

- **Spec + tickets** ([spec](docs/specs/2026-09-04-spec-papel-coordenador.md) · [tickets](docs/specs/2026-09-04-tickets-papel-coordenador.md)): os três acumulam papel (corretor com carteira própria + coordenador de vendas direcionadas). **Um login por pessoa**; o papel troca na tela.
- **Decisão de desenho — nem 4º `usuarios.tipo`, nem `beneficiario`:** `tipo` é coluna única e decide a rota (marcar de coordenador apagaria a carteira própria → forçaria a 2ª conta, proibida); e o beneficiário escopa **por cargo no empreendimento inteiro**, enquanto coordenação escopa **por pessoa** (`vendas.coordenadora_id`) — sob `cargo_beneficiario='Coordenadora'` a Carol veria as 12 da Jessica. Reusa-se o **motor** (`fatiaCargoDoPagamento`), não o tipo de acesso. O vínculo já existia: `coordenadoras.usuario_id` (migr. 030/031). **Zero migration.**
- **Helpers puros novos** (TDD, 26 testes; `comissaoCalculator.js`): `coordenadoraDoUsuario`, `papeisDisponiveis`, `vendasDaCoordenacao` (exclui venda própria — regra da 031 — mais distrato/excluída pela régua única de 28/08), `resumoCoordenacao` (fatia paga/pendente + macro neutro + `vazio`). Suíte: **160/160 verde**. Nenhuma regra de negócio dentro de componente.
- **UI:** seletor de papel só pra quem acumula (default sempre "Corretor", zero regressão) + `PainelCoordenacao.jsx`. **Estado vazio explícito** pro Pires ("nenhuma venda direcionada ainda"), nunca R$ 0,00 mudo. Carga sob demanda e **paginada** — a Carol tem **9.737 parcelas** em 162 vendas; sem `fetchAllPaginated` o total sairia truncado em silêncio.
- **Conferido contra produção (read-only, recompute independente bateu à casa decimal):** Carol 162 vendas no escopo · recebida R$ 100.637,76 · a receber R$ 398.140,52. Jessica 10 · R$ 7.206,70 · R$ 24.460,98. Pires: `papeis=['corretor']` (sem linha em `coordenadoras` ainda) — o seletor não aparece, e é o comportamento correto.
- **Estado dos acessos:** Pires (`9b1f5c90`, cadastro **interno**, 46 vendas sendo **13 externas**) e Jessica (`e94de4d7`, email `jessica@`, não `jessicacararo@`) **já têm login**. **Carolina** (`4c04b405`, "Carolina de Oliveira dos Santos Rita") **não tem** — email do cadastro é placeholder `@sync.local`, criar pelo 🔑 do Admin passando o email real. Existe um 2º cadastro do Pires (`a80c1aa4`, inativo, 0 vendas) — duplicata inerte.
- **Nada foi escrito em produção.** Pendente de decisão do Jonas: email da Jessica · confirmar a Carolina · taxa do Pires em `coordenadoras.percentual_padrao` (sem ela a linha dele não é criada) · escrever `coordenadora_taxa` na criação da venda (hoje só o backfill escreve).
- **Anotado, não corrigido:** o rótulo "Sua Comissão (X%)" do CorretorDashboard usa o percentual do **cadastro** (2,5% pro Pires) mesmo em venda externa. Só o rótulo — o dinheiro já sai certo por `percentualCorretorDaVenda`.
